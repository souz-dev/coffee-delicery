import { styled } from 'styled-components'

export const SimplesButton = styled.button`
  padding: 0.8rem;
  border: none;
  border-radius: 6px;
  display: flex;
  gap: 0.4rem;
  align-items: center;
  justify-content: space-between;
  cursor: pointer;
  outline: none;
`
